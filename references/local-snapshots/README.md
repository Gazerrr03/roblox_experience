# Local Snapshots

This folder stores local Studio snapshot files (`.rbxl`) that are useful for debugging or historical reference.

- These files are references, not source-of-truth runtime code.
- Prefer source-driven changes in `packages/**` and `places/**`.
- Document snapshot intent in PR/task notes when adding new files here.
