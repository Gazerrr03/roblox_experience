export default function SectionDivider() {
  return (
    <div
      className="h-px my-24"
      style={{
        background:
          "linear-gradient(90deg, transparent, var(--text-muted), transparent)",
      }}
    />
  );
}
