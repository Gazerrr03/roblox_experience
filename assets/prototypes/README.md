# Prototypes

This folder stores non-runtime prototype sources used for design exploration.

- Keep `.pen` prototype files and static preview files here.
- Treat this folder as reference input, not runtime code.
- If a prototype graduates to runtime UI/gameplay, implement it in `packages/**` or `places/**` with tests.
